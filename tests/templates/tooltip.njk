{% set page = 'tooltip' %}
{% extends 'parts/layout.njk' %}

{% import 'code/' + page + '-code.njk' as fn %}
{% block main %}
  {% for item in [
    'left',
    'right',
    'top',
    'bottom'
  ] %}
    
    <div>Lorem ipsum dolor sit amet. Quo vitae <span class="tooltip {{ item }}" data-tooltip="I'm a tooltip! Mouse over me to see my content.">{{ item }}</span> odit fuga asperiores.</div>
    <pre><code class="language-scss">
      {%- filter trim %}{{ fn.get(item) }}{% endfilter -%}
    </code></pre>
  {% endfor %}
{% endblock %}